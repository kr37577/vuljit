バージョン
Python 3.12.3

依存関係
requirements.txt

セットアップ手順（venv の例）
- Python の確認: `python3 --version`
- 仮想環境作成: `python3 -m venv .venv`
- 有効化: `source .venv/bin/activate`
- 依存インストール: `pip install -r requirements.txt`
- 依存固定（必要なら）: `pip freeze > requirements.txt` で更新

環境変数の設定
- `.env.example` をコピーして `.env` を作成: `cp .env.example .env`
- `.env` の中で必要な項目を埋める（例: `GITHUB_TOKEN`, `GOOGLE_APPLICATION_CREDENTIALS`, `GCP_PROJECT_ID`, パス類）
- デフォルトのデータパスは `datasets/` 配下（必要に応じて `.env` で上書き）

OSS-Fuzzのレポジトリをクローンした時のハッシュ
96cf5fd552e90b1879d5d3c9bf6d9cdb95e6f122

実行プロセス
1. データセットの準備
2. メトリクスの算出
3. メトリクスの集約
4. モデルの構築・評価
5. シミュレーション